'use strict';

const darray = function(array){
  let x = 0;
  let y = 0;

  for (let i = 0; i < array.length; i++) {
    x = array[i];
        
  } for(let j = 0; j < array.length; j++ ){
    y = array[j];
  }
    if(x && y === 0){
        
    }


};